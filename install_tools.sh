sudo apt install cmake
sudo apt install libxrandr-dev 
sudo apt install libxinerama-dev 
sudo apt install libxcursor-dev 
sudo apt install libxi-dev 

# Go to https://www.glfw.org/ and download the source code.
# There run cmake, followed by make install.
# Now linker flags provided in compile.py should work.
